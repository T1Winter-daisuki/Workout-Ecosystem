# Để RỖNG — mọi fetch thành đường dẫn tương đối /api/..., tự đi qua proxy trong next.config.js
NEXT_PUBLIC_API_URL=

# BE thật mà proxy chuyển tiếp tới (chỉ dùng server-side: next.config.js + middleware.ts)
# API_URL=
API_URL=
